#include <stdio.h>
#include <conio.h>
int main()
{
    int a,b;
    printf("enter the two number for multipling");
    scanf("%d %d", &a, &b);
    printf("product of %d and %d is %d",a,b,a*b);
    getch();

}